<h1>Books</h1> 
<table>
<% @books.each do |book| %>

<tr>    
　<th>Title</th> 
　<sapn><%= book.title %></span>
　<th>Body</th>
　<span><%= book.body %></span>
</tr> 
<% end %>

<tr>    
<td>
  <%= link_to "Show", show_book_path(@books.id) %>
</td>
<td>
  <%= link_to "Edit", edit_book_path(@books.id) %>
</td>
<td>
  <%= link_to "Destroy", destroy_book_path(@books.id), method: :delete, "data-confirm" => "Are you sure?" %>
</td>
</tr>
<% end %>　
</table>
    
<h1>New book</h1>
<% if @book.errors.any? %>
  <%= @book.errors.count %>errors prohibited this book from being saved:
  <ul>
    <% @book.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
  <% end %>
  
<%= form_with model: @books, url: '/books', method: :post do |f| %>

  <h4>Title</h4>
  <%= f.text_field :title %>
  
  <h4>Body</h4>
  <%= f.text_area :body %>
  
  <%= f.submit 'Create Book' %>
<% end %>
    
<!--startクリック後の一覧および投稿画面-->


